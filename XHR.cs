using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using System.Net;

namespace ItzWarty
{
    /*
     * Deprecated, needs housekeeping
    public static class XHR
    {
        private static WebClient webclient = new WebClient();
        public static string GetURL(string url)
        {
            lock (webclient)
            {
                return webclient.DownloadString(url);
            }
        } public static string GetString(string url) { return GetURL(url); }
    }
     */
}
